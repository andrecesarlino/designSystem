{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev" : "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@ignite-ui/eslint-config": "file:packages/eslint-config",
    "turbo": "^1.8.3"
  }
}
